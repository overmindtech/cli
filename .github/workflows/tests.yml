name: Run Tests
on: push
jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    env:
      CGO_ENABLED: 0
    steps:
      - name: Go Init
        uses: ./.github/actions/go_init/action.yml

      - name: Get dependencies
        run: |
          go get -v -t -d ./...

      - name: Go Vet
        run: go vet ./...

      - name: Go Test
        run: |
          go test -v -timeout 5m ./...

  golangci:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - name: Go Init
        uses: ./.github/actions/go_init/action.yml

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v3
        with:
          version: v1.53
